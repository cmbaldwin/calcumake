<div class="d-flex align-items-center gap-2" id="times_printed_<%= pricing.id %>">
    <%= button_to decrement_times_printed_print_pricing_path(pricing),
      method: :patch,
      class: "btn btn-outline-secondary btn-sm",
      title: t('print_pricing.times_printed_decrease'),
      form_class: "d-inline" do %>
    <span class="fw-bold">−</span>
    <% end %>
    <span class="badge bg-primary fs-6 px-3"><%= pricing.times_printed || 0 %></span>
    <%= button_to increment_times_printed_print_pricing_path(pricing),
      method: :patch,
      class: "btn btn-outline-secondary btn-sm",
      title: t('print_pricing.times_printed_increase'),
      form_class: "d-inline" do %>
    <span class="fw-bold">+</span>
    <% end %>
</div>