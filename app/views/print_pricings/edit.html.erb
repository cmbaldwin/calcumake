<% content_for :body_class, "pricing-form-page" %>

<%= render Shared::PageHeaderComponent.new(
  title: t('print_pricing.calculator.edit_title'),
  subtitle: t('print_pricing.calculator.edit_subtitle')
) %>

<%= render 'form', print_pricing: @print_pricing %>
