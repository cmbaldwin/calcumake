<!DOCTYPE html>
<html>

<head>
    <title><%= content_for(:title) || "CalcuMake - 3D Print Cost Calculator" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%# Google AdSense %>
    <meta name="google-adsense-account" content="ca-pub-5257142454834240">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Bootstrap 5 with custom styling %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
</head>

<body class="<%= content_for(:body_class) %>">
    <!-- Flash Messages -->
    <div id="flash" class="flash">
        <%= render "layouts/flash" %>
    </div>

    <%= render "shared/navbar" %>

    <!-- Main Content Container -->
    <div class="container my-4">
        <main>
            <%= yield %>
        </main>
    </div>

    <!-- Footer -->
    <footer class="bg-light mt-5 py-4 border-top">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <p class="mb-1 text-muted">
                        © <%= Date.current.year %> 株式会社モアブ (MOAB Co., Ltd.)
                    </p>
                    <p class="mb-0 small text-muted">
                        <%= t('footer.description') %>
                    </p>
                </div>
                <div class="col-md-6 text-md-end">
                    <div class="d-flex flex-column flex-md-row justify-content-md-end gap-3">
                        <%= link_to t('support.nav_title'), support_path,
                            class: "text-decoration-none small" %>
                        <%= link_to t('legal.privacy_policy.nav_title'), privacy_policy_path,
                            class: "text-decoration-none small" %>
                        <%= link_to t('legal.user_agreement.nav_title'), user_agreement_path,
                            class: "text-decoration-none small" %>
                    </div>
                </div>
            </div>
        </div>
    </footer>
</body>

</html>