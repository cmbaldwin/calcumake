<section class="demo-header py-4 bg-light">
  <div class="container">
    <div class="row align-items-center">
      <div class="col-md-8">
        <h1 class="h2 mb-2">
          <i class="bi bi-calculator-fill text-primary me-2"></i>
          <%= t('demo.title') %>
        </h1>
        <p class="text-muted mb-0">
          <%= t('demo.subtitle') %>
        </p>
      </div>
      <div class="col-md-4 text-md-end">
        <%= link_to t('demo.sign_up_full_version'), new_user_registration_path,
            class: "btn btn-primary",
            onclick: "if(typeof trackSignupStart === 'function') trackSignupStart();" %>
      </div>
    </div>
  </div>
</section>